# Пример для docker-compose: сервис caddy (профиль with-https) и сервис web.
# Скопируй как deploy/Caddyfile и замени домен.

example.com {
    reverse_proxy web:80
}

# Если нужен и www:
# www.example.com {
#     redir https://example.com{uri}
# }
