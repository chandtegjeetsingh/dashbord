# Прямые запросы к FastAPI (без прокси Vite). Скопируйте в .env и поднимите API на :8000.
# VITE_API_BASE=http://127.0.0.1:8000

# Ссылка «Таблица ↗» у карточки «Средняя стоимость доставки» (если не задана — id как у API по умолчанию).
# VITE_DELIVERY_LOGISTICS_SHEET_URL=https://docs.google.com/spreadsheets/d/ВАШ_ID/edit
